<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
>
	<Panel
		class="sapUiNoContentPadding"
		backgroundDesign="Transparent"
	>
		<IconTabBar
			class="sapUiNoContentPadding"
			selectedKey="{Utility>/Function}"
			select="onIconTabChange"
		>
			<items>
				<IconTabFilter
					key="RevocaInvioFirma"
					text="{i18n>iconTabRevocaInvioFirma}"
					icon="sap-icon://list"
					visible="{Utility>/EnableRevocaInvioFirma}"
				/>
				<IconTabSeparator visible="{Utility>/EnableRevocaInvioFirma}"/>
				<IconTabFilter
					key="Annullamento"
					text="{i18n>iconTabAnnullamento}"
					icon="sap-icon://edit"
					visible="{Utility>/EnableAnnullamento}"
				/>
				<IconTabSeparator visible="{Utility>/EnableAnnullamento}"/>
				<IconTabFilter
					key="Rettifica"
					text="{i18n>iconTabEdit}"
					icon="sap-icon://edit"
					visible="{Utility>/EnableEdit}"
				/>
				<IconTabSeparator visible="{Utility>/EnableEdit}"/>
				<IconTabFilter
					key="Dettaglio"
					text="{i18n>iconTabDetail}"
					icon="sap-icon://detail-view"
				/>
				<IconTabSeparator/>
				<IconTabFilter
					key="Workflow"
					text="{i18n>iconTabWorkflow}"
					icon="sap-icon://history"
				/>
				<IconTabSeparator/>
				<IconTabFilter
					text="{i18n>iconTabFascicoloElettronico}"
					icon="sap-icon://documents"
				/>
			</items>
		</IconTabBar>
	</Panel>
</c:FragmentDefinition>